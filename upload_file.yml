---
- hosts: target_group
  become: true
  gather_facts: false
  become_user: root
  become_method: su
  become_exe: sudo su -
  tasks:
    - name: Copy the file to the server using generic paths
      ansible.builtin.copy:
        src: /path/to/local/example_script.sh
        dest: /home/<user>/example_script.sh
        mode: '0755'
